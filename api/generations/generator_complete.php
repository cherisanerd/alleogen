<?php
/**
 * Complete-tier file builders. Stub — real implementation comes in
 * subsequent commits.
 */

declare(strict_types=1);

function generateCompleteFiles(array $ctx): array
{
    throw new RuntimeException(
        'generateCompleteFiles() is not yet implemented. Per-file builders '
        . 'land in subsequent commits.'
    );
}
