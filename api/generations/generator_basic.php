<?php
/**
 * Basic-tier file builders. Stub — real implementation comes in
 * subsequent commits that add one builder file at a time under
 * api/generations/files/ and wires them in here.
 */

declare(strict_types=1);

function generateBasicFiles(array $ctx): array
{
    throw new RuntimeException(
        'generateBasicFiles() is not yet implemented. Per-file builders '
        . 'land in subsequent commits.'
    );
}
